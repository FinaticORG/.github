name: CI

on:
  pull_request:
  push:
    branches: [develop, staging, main]

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/reusable-actionlint.yml
    with:
      strict: true
